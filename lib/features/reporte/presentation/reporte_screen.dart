import 'package:flutter/material.dart';

class ReporteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Reporte', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
