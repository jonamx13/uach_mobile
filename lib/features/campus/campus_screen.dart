import 'package:flutter/material.dart';

class CampusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Campus', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
